eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
server:
  port: 8011
spring:
  application:
    name: spring-cloud-consumer
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: spring-cloud-config
      #profile: dev
      #label: hxd